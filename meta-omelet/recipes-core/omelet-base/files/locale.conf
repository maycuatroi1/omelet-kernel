# System-wide default locale (read by systemd / PAM for services and logins).
# C.UTF-8 is built into glibc (>=2.35, scarthgap ships 2.39), so it needs no
# locale generation and is always present. A UTF-8 LC_CTYPE keeps the shell and
# opencode agreeing on UTF-8 for I/O.
LANG=C.UTF-8
