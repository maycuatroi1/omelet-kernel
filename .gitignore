# ---- Yocto / kas: cloned layers + build artifacts (do NOT commit) ----
/poky/
/meta-openembedded/
/meta-raspberrypi/
/build/
/downloads/
/sstate-cache/
*.lock
.kas*

# Flashable images / kernel blobs
*.wic
*.wic.bz2
*.wic.bmap
*.img
*.bin
*.ko

# ---- Buildroot artifacts (legacy) ----
output/
dl/

# Secrets / personal configs - KHONG commit
*.key
wg0.conf
secrets/
.env

# Built images copied out of the volume
/deploy/

# QEMU test artifacts (extracted kernel/dtb + padded SD image)
/qemu/
.claude/skills/learned/.processed/
